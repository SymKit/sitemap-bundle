grumphp:
    tasks:
        phpstan:
            config: phpstan.neon.dist
            memory_limit: 512M
        phpcsfixer:
            config: .php-cs-fixer.dist.php
        phpunit:
            config: phpunit.xml.dist
        shell:
            scripts:
                - ['composer', 'audit']
